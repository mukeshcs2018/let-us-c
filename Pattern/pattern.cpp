#include "iostream"
using namespace std;
int main()
{
  int num=4, row, col, space,  count=1;
  float i = -15;
      for(space = 1; space <= (num-row); space++)
      {
       cout<<" ";
          for(col = 1; col <= row; col++)
          {
            cout<<count++;
          }
      i = i+0.5;
    cout<<"\n";
      }
  }
